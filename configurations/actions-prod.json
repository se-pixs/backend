{
  "title": "Actions Prod",
  "description": "Actions that can be performed on productive instance",
  "actions": [
    {
      "name": "upload",
      "description": "Upload a file",
      "helpMessage": "",
      "path": "/upload",
      "format": {
        "type": "string",
        "enum": [
          "JPEG",
          "PNG"
        ]
      },
      "source": {
        "type": "string"
      }
    },
    {
      "name": "download",
      "description": "Download a file",
      "helpMessage": "",
      "path": "/download",
      "format": {
        "type": "string",
        "enum": [
          "JPEG",
          "PNG"
        ]
      }
    }
  ]
}