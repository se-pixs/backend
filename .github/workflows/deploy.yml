    - name: Deploy Stage
      uses: actions/github-script@v5
      with:
        script: |
          await github.rest.actions.createWorkflowDispatch({
            'Mause',
            'fdroidrepo',
            'workflows/update.yaml',
            'main',
          });
