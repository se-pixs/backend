
name: Call a reusable workflow1

on:
  push:
    branches:
      - deploy

jobs:
  call-workflow:
    uses: se-pixs/frontend/.github/workflows/deploy.yml@deploy

  call-workflow-passing-data:
    uses: se-pixs/frontend/.github/workflows/deploy.yml@deploy
    with:
      username: der2beers
    secrets:
      token: ${{ secrets.TOKEN }}
